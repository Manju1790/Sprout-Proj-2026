import { describe, it, expect } from "vitest";

describe("SupportAI", () => {
  it("has the expected product name", () => {
    expect("Smart Support Ticket Resolution").toContain("Support");
  });
});
